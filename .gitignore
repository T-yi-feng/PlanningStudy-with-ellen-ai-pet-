# 本地用户数据：个人计划、专注统计、历史记录 —— 不入库
data.json

# API 密钥本地文件：包含智谱等模型 API Key —— 不入库
secret.json

# Python 缓存与临时文件
__pycache__/
*.py[cod]
*.tmp

# 数据备份
data.json.bak

# 本地大文件模型包（几 GB 的 GPT-SoVITS 整合包等）—— 不入库
model/

# PyInstaller 构建产物（几十 MB 的 exe 与中间文件）—— 不入库
build/
dist/
