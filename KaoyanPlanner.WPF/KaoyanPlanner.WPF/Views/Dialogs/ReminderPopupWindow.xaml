<Window x:Class="KaoyanPlanner.WPF.Views.Dialogs.ReminderPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提醒" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        Topmost="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border x:Name="card" Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            CornerRadius="10" MinWidth="300" MaxWidth="380"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <!-- 顶部 4px 强调条 -->
            <Border Height="4" CornerRadius="9,9,0,0" Background="{StaticResource AccentBrush}" />
            <TextBlock x:Name="titleText" Grid.Row="1" Style="{StaticResource TitleText}"
                       FontSize="15" Margin="18,12,18,2" />
            <TextBlock x:Name="messageText" Grid.Row="2" Style="{StaticResource BodyText}"
                       Margin="18,2,18,16" TextWrapping="Wrap" />
        </Grid>
    </Border>
</Window>
